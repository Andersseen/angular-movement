# Spec NNN — <short title>

- **Status:** draft | approved | in-progress | done | blocked
- **Created:** YYYY-MM-DD
- **Last updated:** YYYY-MM-DD
- **Breaks public API:** yes / no ← if yes, requires explicit user approval
- **Related:** (roadmap item, issue #, previous spec)

## Problem / motivation

<What is wrong or missing today, and why it matters. 2–5 sentences. Link to CONTEXT.md goals if relevant.>

## Proposed solution

<What will exist after this task that doesn't exist now. Describe behavior, not implementation.
For API changes: show the exact template/TS usage the user will write.>

```html
<!-- Example of the resulting API, if applicable -->
```

## Out of scope

<Explicit list of things this spec does NOT change. Files you won't touch, behaviors you won't alter,
refactors you're deliberately skipping.>

## Acceptance criteria

<Each item must be objectively checkable — by a command, a test, or a specific manual step.>

- [ ] ...
- [ ] ...
- [ ] Unit tests cover the new behavior (name the spec file)
- [ ] `pnpm test:coverage`, `ng lint`, `pnpm build` all pass
- [ ] CHANGELOG.md (Unreleased) and docs/ai/STATE.md updated

## Implementation plan

<Ordered steps with exact file paths. Tick as you complete them — this is the resume point if a session dies.>

- [ ] 1. `projects/movement/src/lib/...` — ...
- [ ] 2. ...

## Verification notes

<How it was actually verified: commands run + results, routes manually checked, edge cases exercised.>

## Follow-ups (out of scope, noted for later)

- ...
